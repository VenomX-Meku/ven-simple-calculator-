<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base theme -->
    <style name="Base.Theme.VenomLogin" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Apply colors from colors.xml -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/buttonPressedColor</item>
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="android:windowBackground">@color/backgroundColor</item>
        <item name="colorOnPrimary">@color/textColor</item>
        <item name="colorOnSecondary">@color/textColor</item>
    </style>

    <!-- App theme -->
    <style name="Theme.VenomLogin" parent="Base.Theme.VenomLogin" />
</resources>
